import React from "react"

const Logo = () => {
  return (
    <img
      alt="stones-throw-logo"
      src={require("../images/logo-white.png")}
      width="55"
      className="logo"
    />
  )
}

export default Logo
